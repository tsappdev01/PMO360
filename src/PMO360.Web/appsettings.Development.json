{
  "//Authentication": "Development runs with no sign-in, so the portal needs no app registration to start: the dashboard opens directly and a banner says so on every page. Set EnableSso to true here once AzureAd:TenantId and AzureAd:ClientId are real, to test the Entra ID flow.",
  "Authentication": {
    "EnableSso": false
  },

  "//Logging": "Raise PMO360 to Debug to see full exception detail, including stack traces, when investigating a failure.",
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "PMO360": "Information"
    }
  },
  "Notifications": {
    "Enabled": false,
    "RedirectAllTo": "your.name@dubaiinvestments.com"
  },
  "ConnectionStrings": {
    "PmoDatabase": "Server=UATWEB01,1433;Initial Catalog=PMO360;User Id=bpuser;Password=bpuser;Encrypt=True;TrustServerCertificate=True;"
  }
}
